// make a func that will return a greet statement such as
// "Hello, <name> how are you doing today?"

package main

func Greet(name string) string {
	return "Hello, " + name + ",how are you doing today?"
}
